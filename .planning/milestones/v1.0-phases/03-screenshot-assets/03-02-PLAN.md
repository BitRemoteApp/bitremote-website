---
phase: 03-screenshot-assets
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - screenshots-source/light/iphone-home.png
  - screenshots-source/dark/iphone-home.png
  - public/screenshots/light/iphone-home.webp
  - public/screenshots/light/iphone-home@2x.webp
  - public/screenshots/dark/iphone-home.webp
  - public/screenshots/dark/iphone-home@2x.webp
autonomous: false
requirements:
  - TECH-03
  - SHOW-01
  - SHOW-02
  - SHOW-03
  - SHOW-04

user_setup:
  - service: iOS Simulator
    why: "Capture app screenshots in both light and dark appearance"
    dashboard_config:
      - task: "Take screenshot of BitRemote home screen in light appearance"
        location: "Xcode > iOS Simulator > Features > Appearance > Light, then Cmd+S"
      - task: "Take screenshot of BitRemote home screen in dark appearance"
        location: "Xcode > iOS Simulator > Features > Appearance > Dark, then Cmd+S"

must_haves:
  truths:
    - "WebP files exist in public/screenshots/light/ and public/screenshots/dark/ at both 1x and 2x sizes"
    - "Each 1x WebP is under 100KB"
    - "The showcase section visually displays a real app screenshot inside an iPhone device frame"
    - "Toggling OS dark/light mode switches the displayed screenshot"
    - "Screenshots animate into view on scroll"
  artifacts:
    - path: "public/screenshots/light/iphone-home.webp"
      provides: "1x light mode WebP screenshot"
    - path: "public/screenshots/light/iphone-home@2x.webp"
      provides: "2x light mode WebP screenshot"
    - path: "public/screenshots/dark/iphone-home.webp"
      provides: "1x dark mode WebP screenshot"
    - path: "public/screenshots/dark/iphone-home@2x.webp"
      provides: "2x dark mode WebP screenshot"
  key_links:
    - from: "public/screenshots/"
      to: "src/components/AppScreenshot.tsx"
      via: "srcSet paths in picture element match file names on disk"
      pattern: "iphone-home"
---

<objective>
Capture real app screenshots, run the optimization pipeline, and verify the complete showcase experience end-to-end.

Purpose: The components from Plan 01 are wired up but render empty/broken images without real WebP assets on disk. This plan closes the loop by getting actual screenshots from the user, processing them through the sharp pipeline, and verifying the full visual result.

Output: 4 WebP files (1x + 2x for light and dark), verified visual showcase
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-screenshot-assets/03-01-SUMMARY.md
@.planning/phases/03-screenshot-assets/03-UI-SPEC.md

@scripts/optimize-screenshots.mjs
@src/components/AppScreenshot.tsx
@src/components/AppShowcaseClient.tsx
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: User provides app screenshots</name>
  <files>screenshots-source/light/iphone-home.png, screenshots-source/dark/iphone-home.png</files>
  <action>
    The user must capture screenshots of the BitRemote app from iOS Simulator (or a real device) in both light and dark appearance modes.

    Required files:
    - `screenshots-source/light/iphone-home.png` — BitRemote home screen in light appearance
    - `screenshots-source/dark/iphone-home.png` — BitRemote home screen in dark appearance

    The PNG files should be full-resolution Simulator captures (typically 1170x2532 for iPhone 15 Pro or similar). The optimization script will handle resizing and WebP conversion.

    Steps:
    1. Open BitRemote in iOS Simulator
    2. Set appearance to Light (Features > Appearance > Light)
    3. Navigate to the home screen showing active downloads or main UI
    4. Press Cmd+S to save screenshot
    5. Move the PNG to `screenshots-source/light/iphone-home.png`
    6. Set appearance to Dark (Features > Appearance > Dark)
    7. Press Cmd+S again
    8. Move the PNG to `screenshots-source/dark/iphone-home.png`
  </action>
  <verify>
    <automated>test -f screenshots-source/light/iphone-home.png && test -f screenshots-source/dark/iphone-home.png && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `screenshots-source/light/iphone-home.png` exists and is a valid PNG (file size > 10KB)
    - `screenshots-source/dark/iphone-home.png` exists and is a valid PNG (file size > 10KB)
  </acceptance_criteria>
  <resume-signal>Type "screenshots ready" when both PNGs are in place</resume-signal>
  <done>Both light and dark source PNGs exist in screenshots-source/ ready for optimization</done>
</task>

<task type="auto">
  <name>Task 2: Run optimization pipeline and verify WebP output</name>
  <files>public/screenshots/light/iphone-home.webp, public/screenshots/light/iphone-home@2x.webp, public/screenshots/dark/iphone-home.webp, public/screenshots/dark/iphone-home@2x.webp</files>
  <read_first>
    - scripts/optimize-screenshots.mjs (the script to run)
    - src/components/AppScreenshot.tsx (srcSet paths that must match output filenames)
  </read_first>
  <action>
    1. Run the optimization script:
       ```bash
       node scripts/optimize-screenshots.mjs
       ```
       Expected console output:
       - `[light] iphone-home: 1x + 2x WebP written`
       - `[dark] iphone-home: 1x + 2x WebP written`

    2. Verify output files exist:
       ```bash
       ls -la public/screenshots/light/iphone-home.webp public/screenshots/light/iphone-home@2x.webp
       ls -la public/screenshots/dark/iphone-home.webp public/screenshots/dark/iphone-home@2x.webp
       ```

    3. Verify 1x file sizes are under 100KB (performance budget from UI-SPEC):
       ```bash
       stat -f%z public/screenshots/light/iphone-home.webp  # must be < 102400
       stat -f%z public/screenshots/dark/iphone-home.webp   # must be < 102400
       ```
       If a 1x file exceeds 100KB, re-run the script with reduced quality (lower WEBP_QUALITY constant to 80 or 75) until within budget.

    4. Run full build to verify static export works with new assets:
       ```bash
       npm run build
       ```

    5. Verify the WebP files are correctly sized (1x should be 390px wide, 2x should be 780px wide):
       ```bash
       node -e "const sharp = require('sharp'); sharp('public/screenshots/light/iphone-home.webp').metadata().then(m => console.log('1x width:', m.width))"
       node -e "const sharp = require('sharp'); sharp('public/screenshots/light/iphone-home@2x.webp').metadata().then(m => console.log('2x width:', m.width))"
       ```
       Expected: 1x width = 390, 2x width = 780.
  </action>
  <verify>
    <automated>test -f public/screenshots/light/iphone-home.webp && test -f public/screenshots/dark/iphone-home.webp && test -f public/screenshots/light/iphone-home@2x.webp && test -f public/screenshots/dark/iphone-home@2x.webp && npm run build 2>&1 | tail -3 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `public/screenshots/light/iphone-home.webp` exists and is under 100KB
    - `public/screenshots/light/iphone-home@2x.webp` exists
    - `public/screenshots/dark/iphone-home.webp` exists and is under 100KB
    - `public/screenshots/dark/iphone-home@2x.webp` exists
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>All 4 WebP files exist at correct dimensions (390px 1x, 780px 2x), within performance budget, and the static build succeeds</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of showcase section</name>
  <files>src/components/AppShowcaseClient.tsx</files>
  <action>
    Start dev server and verify the complete showcase experience:
    1. Run `npm run dev`
    2. Open http://localhost:3000/en/ in browser
    3. Verify all visual criteria listed in how-to-verify below
    4. If issues found, fix component CSS or layout before marking approved
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3</automated>
  </verify>
  <what-built>Complete app showcase section with real screenshots inside iPhone device frames, CSS-native dark/light switching, and scroll entrance animation</what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Open http://localhost:3000/en/ in browser
    3. Scroll down past the hero section — verify:
       - An iPhone device frame is visible with a real app screenshot inside
       - The frame has rounded corners, a notch, volume buttons, and power button
       - The screenshot fades in with a smooth animation as you scroll to it
    4. Open DevTools > Rendering > Emulate CSS media: prefers-color-scheme: dark
       - Verify the screenshot inside the frame switches to the dark variant
    5. Toggle back to light — verify it switches back
    6. Open DevTools > Rendering > Emulate CSS media: prefers-reduced-motion: reduce
       - Reload page and scroll — verify NO animation plays (screenshot is immediately visible)
    7. Check mobile layout: resize browser to 375px width — verify frame is centered and not clipped
  </how-to-verify>
  <acceptance_criteria>
    - Screenshot displays inside device frame at all viewport widths
    - Dark/light switching works via OS preference (no page reload needed, no JS flash)
    - Scroll animation triggers on first scroll into view
    - Reduced motion preference disables all animation
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe any visual issues</resume-signal>
  <done>Showcase section visually approved: device frame renders correctly, dark/light switching works via CSS, scroll animation plays, reduced-motion is respected</done>
</task>

</tasks>

<verification>
- All 4 WebP files exist in `public/screenshots/` at correct 1x/2x dimensions
- 1x WebP files are under 100KB each
- `npm run build` succeeds with the new assets
- Visual: Device frame looks like a realistic iPhone around the screenshot
- Visual: Dark OS mode shows dark screenshot, light shows light — no JS flash
- Visual: Scroll animation fades in the showcase section
- Visual: Mobile layout (375px) is usable — frame centered, not clipped
</verification>

<success_criteria>
- Real app screenshots are optimized to WebP and committed to public/screenshots/
- The showcase section displays correctly in the browser with device frames
- Dark/light theme switching works via CSS prefers-color-scheme on the picture element
- Scroll entrance animation plays using FadeInSection stagger variants
- Performance budget met: 1x WebP files under 100KB
- Static build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-screenshot-assets/03-02-SUMMARY.md`
</output>

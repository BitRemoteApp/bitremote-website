---
phase: 05-visual-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified: []
autonomous: false
requirements:
  - VFX-03
  - HERO-02

must_haves:
  truths:
    - "Site renders correctly at 375px mobile width — no horizontal scroll, no overflow, touch targets >= 44px"
    - "Site renders correctly at 768px tablet width — bento grid 2-column, showcase visible"
    - "Site renders correctly at 1280px desktop width — max-width container respected, glow centered"
    - "SvgDivider gradient fade is visible at all three viewport widths"
    - "Hero glow is visible in dark mode and subtle in light mode without visual artifacts"
    - "Japanese locale (/ja/) renders without text overflow at 375px"
  artifacts: []
  key_links: []
---

<objective>
Verify the visual polish from Plan 01 renders correctly across all viewport sizes and themes, and audit responsive behavior site-wide.

Purpose: Confirm the site meets the Phase 5 success criteria — "The site renders correctly and looks intentional on mobile (375px), tablet (768px), and desktop (1280px+) screen widths" — and that VFX-03 dividers and HERO-02 glow look correct visually.
Output: Visual verification sign-off or issue list for fixes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-visual-polish/05-RESEARCH.md
@.planning/phases/05-visual-polish/05-UI-SPEC.md
@.planning/phases/05-visual-polish/05-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification across viewports and themes</name>
  <files>none — verification only</files>
  <read_first>
    - .planning/phases/05-visual-polish/05-UI-SPEC.md (Interaction States section — viewport checklist)
    - .planning/phases/05-visual-polish/05-RESEARCH.md (Pattern 4: Responsive Audit Protocol)
    - .planning/phases/05-visual-polish/05-01-SUMMARY.md (what was implemented)
  </read_first>
  <action>
    Start the dev server with `npm run dev`. Present the following verification checklist to the user for manual browser inspection in DevTools responsive mode.

    **Dark mode verification (set OS to dark theme):**

    1. Visit http://localhost:3000/en/ at 375px width:
       - No horizontal scrollbar appears
       - Hero glow is visible as a subtle blue halo behind the wordmark/tagline
       - SvgDivider lines show gradient fade (transparent edges, solid center)
       - Bento grid is single-column
       - All touch targets (buttons, links) are at least 44px
       - Nav is accessible and not truncated

    2. At 768px width:
       - Bento grid is 2-column
       - SvgDivider spans full content width with visible gradient
       - App showcase section displays correctly

    3. At 1280px width:
       - Content is centered within max-width container (72rem)
       - Hero glow is centered behind hero content
       - No layout overflow

    **Light mode verification (set OS to light theme):**

    4. Visit http://localhost:3000/en/ at 375px width:
       - Hero glow is barely perceptible (not a blue wash)
       - SvgDivider lines are visible using border color
       - No horizontal scrollbar

    **Japanese locale verification:**

    5. Visit http://localhost:3000/ja/ at 375px width:
       - Hero tagline wraps cleanly (no overflow, no more than 3 lines)
       - SvgDivider lines render correctly
       - All content is visible without horizontal scroll

    **Downloader landing page verification:**

    6. Visit any downloader landing page (e.g., http://localhost:3000/en/downloaders/aria2/):
       - SvgDivider replaces old hr with gradient fade
       - Spacing (my-12) is preserved around dividers
  </action>
  <verify>
    <automated>cd /Users/tatsuzo/Fork/bitremote-website && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - User confirms no horizontal scrollbar at 375px
    - User confirms hero glow visible in dark mode, subtle in light mode
    - User confirms SvgDivider gradient visible at all viewports
    - User confirms Japanese locale renders without overflow at 375px
    - User confirms downloader landing page dividers are updated
  </acceptance_criteria>
  <done>Visual verification complete — site renders correctly at all three viewport widths in both themes, VFX-03 dividers and HERO-02 glow confirmed visually correct.</done>
</task>

</tasks>

<verification>
- Human visual inspection confirms all checkpoints pass
- No responsive layout issues identified
- Both dark and light themes verified
- Non-English locale (Japanese) verified at mobile width
</verification>

<success_criteria>
1. User approves visual appearance at 375px, 768px, and 1280px
2. Hero glow confirmed visible in dark mode, subtle in light mode
3. SvgDivider gradient confirmed visible and elegant
4. No horizontal scrollbar on any viewport
5. Japanese locale renders correctly at mobile width
</success_criteria>

<output>
After completion, create `.planning/phases/05-visual-polish/05-02-SUMMARY.md`
</output>

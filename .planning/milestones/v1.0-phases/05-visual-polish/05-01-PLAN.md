---
phase: 05-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SvgDivider.tsx
  - src/app/[locale]/page.tsx
  - src/components/DownloaderLandingPage.tsx
  - src/app/globals.css
  - src/components/HeroSection.tsx
autonomous: true
requirements:
  - VFX-03
  - HERO-02

must_haves:
  truths:
    - "Section boundaries use thin gradient-fading divider lines instead of plain <hr> borders"
    - "The hero section has a visible ambient blue glow in dark mode and a subtle barely-perceptible glow in light mode"
    - "No <hr> elements remain anywhere on the site"
    - "The glow does not cause horizontal scrollbar on any viewport width"
  artifacts:
    - path: "src/components/SvgDivider.tsx"
      provides: "CSS gradient divider component replacing <hr>"
      exports: ["SvgDivider"]
    - path: "src/app/globals.css"
      provides: "--hero-glow-color CSS variable with color-mix for dark/light"
      contains: "--hero-glow-color"
    - path: "src/components/HeroSection.tsx"
      provides: "Ambient glow div inside hero section"
      contains: "hero-glow-color"
  key_links:
    - from: "src/app/[locale]/page.tsx"
      to: "src/components/SvgDivider.tsx"
      via: "import { SvgDivider }"
      pattern: "import.*SvgDivider.*from"
    - from: "src/components/DownloaderLandingPage.tsx"
      to: "src/components/SvgDivider.tsx"
      via: "import { SvgDivider }"
      pattern: "import.*SvgDivider.*from"
    - from: "src/components/HeroSection.tsx"
      to: "src/app/globals.css"
      via: "var(--hero-glow-color) in radial-gradient"
      pattern: "hero-glow-color"
---

<objective>
Create the SvgDivider component and hero ambient glow effect — the two visual polish features that complete VFX-03 and HERO-02.

Purpose: Replace all plain `<hr>` section dividers with elegant gradient-fading lines, and add a decorative ambient blue glow behind the hero section that adapts to dark/light themes via CSS custom properties.
Output: SvgDivider component, updated page files with SvgDivider replacing `<hr>`, hero glow token in globals.css, glow div in HeroSection.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-visual-polish/05-RESEARCH.md
@.planning/phases/05-visual-polish/05-UI-SPEC.md

@src/components/HeroSection.tsx
@src/app/globals.css
@src/app/[locale]/page.tsx
@src/components/DownloaderLandingPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SvgDivider component and replace all hr elements</name>
  <files>src/components/SvgDivider.tsx, src/app/[locale]/page.tsx, src/components/DownloaderLandingPage.tsx</files>
  <read_first>
    - src/app/[locale]/page.tsx (see current 5x <hr> locations at lines 74, 78, 87, 109, 134)
    - src/components/DownloaderLandingPage.tsx (see current 2x <hr> at lines 38, 46)
    - .planning/phases/05-visual-polish/05-UI-SPEC.md (VFX-03 spec section)
    - .planning/phases/05-visual-polish/05-RESEARCH.md (Pattern 2: CSS-Only Divider)
  </read_first>
  <action>
1. Create `src/components/SvgDivider.tsx` as a server component (no 'use client' directive). Named export `SvgDivider`.

```tsx
type Props = {
  className?: string;
};

export function SvgDivider({ className }: Props) {
  return (
    <div
      aria-hidden="true"
      role="presentation"
      className={
        'h-px bg-[linear-gradient(to_right,transparent,var(--color-border)_25%,var(--color-border)_75%,transparent)]' +
        (className ? ' ' + className : '')
      }
    />
  );
}
```

This is a CSS `linear-gradient` on a `<div>`, NOT an SVG element. The name `SvgDivider` is kept for semantic clarity per the UI spec. The gradient fades from transparent to `var(--color-border)` at 25%, holds solid through 75%, and fades back to transparent. Height is 1px. `aria-hidden="true"` and `role="presentation"` mark it as purely decorative.

2. In `src/app/[locale]/page.tsx`:
   - Add import: `import { SvgDivider } from '@/components/SvgDivider';`
   - Replace all 5 instances of `<hr className="border-t border-border" />` with `<SvgDivider />`
   - Lines to replace: 74, 78, 87, 109, 134

3. In `src/components/DownloaderLandingPage.tsx`:
   - Add import: `import { SvgDivider } from '@/components/SvgDivider';`
   - Replace both instances of `<hr className="border-t border-border my-12" />` with `<SvgDivider className="my-12" />`
   - Lines to replace: 38, 46
  </action>
  <verify>
    <automated>cd /Users/tatsuzo/Fork/bitremote-website && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/SvgDivider.tsx exists and contains `export function SvgDivider`
    - src/components/SvgDivider.tsx contains `aria-hidden="true"`
    - src/components/SvgDivider.tsx contains `role="presentation"`
    - src/components/SvgDivider.tsx contains `linear-gradient(to_right,transparent,var(--color-border)_25%,var(--color-border)_75%,transparent)`
    - src/components/SvgDivider.tsx does NOT contain `'use client'`
    - src/app/[locale]/page.tsx contains `import { SvgDivider } from '@/components/SvgDivider'`
    - src/app/[locale]/page.tsx does NOT contain `<hr`
    - src/components/DownloaderLandingPage.tsx contains `import { SvgDivider } from '@/components/SvgDivider'`
    - src/components/DownloaderLandingPage.tsx does NOT contain `<hr`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All 7 hr elements across the site replaced with SvgDivider. No hr elements remain. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add hero ambient glow with theme-adaptive CSS token</name>
  <files>src/app/globals.css, src/components/HeroSection.tsx</files>
  <read_first>
    - src/app/globals.css (see existing CSS variable structure, color-mix pattern at lines 42-44)
    - src/components/HeroSection.tsx (see current section markup, no relative positioning)
    - .planning/phases/05-visual-polish/05-UI-SPEC.md (HERO-02 spec section)
    - .planning/phases/05-visual-polish/05-RESEARCH.md (Pattern 3: Hero Ambient Glow, Option B)
  </read_first>
  <action>
1. In `src/app/globals.css`, add `--hero-glow-color` token:

   In the `:root` block (after the `--bg-panel-88` line, approximately line 44), add:
   ```css
   --hero-glow-color: color-mix(in srgb, var(--color-accent) 6%, transparent);
   ```

   In the `@media (prefers-color-scheme: dark)` `:root` block (after the `--shadow-raise` line, approximately line 63), add:
   ```css
   --hero-glow-color: color-mix(in srgb, var(--color-accent) 14%, transparent);
   ```

   This follows the identical `color-mix` pattern used by `--bg-glass-92`, `--bg-glass-95`, and `--bg-panel-88`.

2. In `src/components/HeroSection.tsx`, modify the `<section>` element and add the glow:

   - Add `relative` to the section className: `className="relative flex flex-col gap-6"`
   - Insert the ambient glow as the first child inside `<section>`, before `<BitRemoteWordmark />`:

   ```tsx
   {/* Ambient glow — decorative only */}
   <div aria-hidden="true" className="pointer-events-none absolute inset-0 overflow-hidden">
     <div
       className="absolute -top-1/4 left-1/2 -translate-x-1/2 w-[80%] max-w-[600px] aspect-[3/2]"
       style={{
         background: 'radial-gradient(ellipse at center, var(--hero-glow-color) 0%, transparent 70%)',
         filter: 'blur(60px)',
       }}
     />
   </div>
   ```

   The outer div uses `absolute inset-0 overflow-hidden` to clip any glow bleed and prevent horizontal scrollbar. The inner div creates the radial gradient orb using `--hero-glow-color` which automatically adapts between dark (14% accent) and light (6% accent) themes. `filter: blur(60px)` softens the gradient edges.

   No `'use client'` directive needed — this is a static CSS effect with no JavaScript, no animation, no interaction.
  </action>
  <verify>
    <automated>cd /Users/tatsuzo/Fork/bitremote-website && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/app/globals.css contains `--hero-glow-color: color-mix(in srgb, var(--color-accent) 6%, transparent)` in the :root block
    - src/app/globals.css contains `--hero-glow-color: color-mix(in srgb, var(--color-accent) 14%, transparent)` in the dark media query block
    - src/components/HeroSection.tsx contains `className="relative flex flex-col gap-6"`
    - src/components/HeroSection.tsx contains `aria-hidden="true"` (on glow wrapper)
    - src/components/HeroSection.tsx contains `pointer-events-none absolute inset-0 overflow-hidden`
    - src/components/HeroSection.tsx contains `radial-gradient(ellipse at center, var(--hero-glow-color)`
    - src/components/HeroSection.tsx contains `filter: 'blur(60px)'`
    - src/components/HeroSection.tsx does NOT contain `'use client'`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Hero section has ambient glow that renders as blue halo in dark mode (14% opacity) and barely-perceptible tint in light mode (6% opacity). No horizontal scrollbar on any viewport. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run lint` passes with 0 warnings
- `npm run build` produces static export in `out/` with all locale paths
- No `<hr` elements exist anywhere in `src/` (grep confirms)
- `globals.css` has `--hero-glow-color` in both light and dark blocks
- `HeroSection.tsx` has `relative` on section and glow div as first child
</verification>

<success_criteria>
1. SvgDivider component exists at src/components/SvgDivider.tsx using CSS linear-gradient
2. All 7 hr elements across the site are replaced with SvgDivider
3. Hero section has ambient glow via --hero-glow-color CSS custom property
4. Glow adapts between dark (14%) and light (6%) themes via color-mix
5. Glow is clipped by overflow-hidden wrapper — no horizontal scrollbar
6. Build passes with zero errors and zero warnings
</success_criteria>

<output>
After completion, create `.planning/phases/05-visual-polish/05-01-SUMMARY.md`
</output>

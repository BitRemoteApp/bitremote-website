---
phase: 06-corrective-total-ui-refresh
plan: 02
type: execute
wave: 1
depends_on:
  - 06-01-PLAN.md
files_modified:
  - src/components/TextTabsNav.tsx
  - src/app/[locale]/layout.tsx
  - src/app/[locale]/page.tsx
autonomous: true
requirements:
  - NAV-04
  - DSGN-08
  - BRAND-01

must_haves:
  truths:
    - "Navigation has persistent active states and no longer depends on hover-only discoverability"
    - "The locale switcher uses calm selected-state treatment rather than bracket-wrapped menu rows"
    - "Homepage information architecture follows the Phase 6 order and includes a top-level Downloaders anchor"
    - "The shared layout reads as one composed product page instead of a set of equally weighted divider-separated blocks"
  artifacts:
    - path: "src/components/TextTabsNav.tsx"
      provides: "Corrected sticky navigation with active state, locale picker, App Store CTA, and downloaders anchor"
      exports: ["TextTabsNav"]
    - path: "src/app/[locale]/page.tsx"
      provides: "Reordered homepage structure aligned with the Phase 6 UI contract"
      contains: "id=\"downloaders\""
    - path: "src/app/[locale]/layout.tsx"
      provides: "Shared layout updated as needed to match the corrected nav and footer language"
      contains: "TextTabsNav"
  key_links:
    - from: "src/components/TextTabsNav.tsx"
      to: "src/app/[locale]/page.tsx"
      via: "anchors target homepage section ids including downloaders"
      pattern: "#downloaders"
    - from: "src/app/[locale]/layout.tsx"
      to: "src/components/TextTabsNav.tsx"
      via: "locale layout continues to render the corrected nav"
      pattern: "<TextTabsNav"
---

<objective>
Rebuild the navigation and homepage skeleton so Phase 6 stops preserving the old screenshot-first hierarchy.

Purpose: Correct the top-level information architecture and shared navigation behavior before the new proof/content sections are added.
Output: corrected sticky nav, calmer locale picker, updated anchor structure, and homepage section order matching the UI contract.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.impeccable.md
@.planning/ROADMAP.md
@.planning/phases/06-corrective-total-ui-refresh/06-CONTEXT.md
@.planning/phases/06-corrective-total-ui-refresh/06-RESEARCH.md
@.planning/phases/06-corrective-total-ui-refresh/06-UI-SPEC.md

@src/components/TextTabsNav.tsx
@src/app/[locale]/layout.tsx
@src/app/[locale]/page.tsx
@src/components/HeroSection.tsx
@src/components/SvgDivider.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign navigation and locale switching around clear state, not hover theatrics</name>
  <files>src/components/TextTabsNav.tsx, src/app/[locale]/layout.tsx</files>
  <read_first>
    - .planning/phases/06-corrective-total-ui-refresh/06-UI-SPEC.md
    - src/components/TextTabsNav.tsx
    - src/app/[locale]/layout.tsx
    - src/messages/en.json
  </read_first>
  <action>
1. Redesign `TextTabsNav` so the current section/page has a visible persistent active state.
2. Add the Downloaders anchor to the primary nav and ensure it targets `#downloaders`.
3. Redesign the locale picker into a calmer rounded panel/list treatment with a visible selected state.
4. Remove bracket wrappers, hover-only reveal logic, and any explicit intent to suppress active styling.
5. Preserve existing behaviors that matter:
   - sticky positioning,
   - outside-click close,
   - Escape close,
   - locale-aware path generation,
   - no-JS fallback via `noscript`.
6. Update `src/app/[locale]/layout.tsx` only as needed to align footer/shared layout language with the corrected nav treatment.
  </action>
  <verify>
    <automated>cd /Users/tatsuzo/Fork/bitremote-website && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/TextTabsNav.tsx` contains `#downloaders`
    - `src/components/TextTabsNav.tsx` does not contain the comment about active states intentionally never being applied
    - `src/components/TextTabsNav.tsx` does not render decorative bracket wrappers for locale labels
    - current locale still sets `aria-current="page"`
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>Navigation and locale switching communicate clear structure and state without retro UI patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Reorder the homepage around hero, product proof, and downloader trust</name>
  <files>src/app/[locale]/page.tsx</files>
  <read_first>
    - .planning/phases/06-corrective-total-ui-refresh/06-CONTEXT.md
    - .planning/phases/06-corrective-total-ui-refresh/06-UI-SPEC.md
    - src/app/[locale]/page.tsx
    - src/components/AppShowcaseClient.tsx
  </read_first>
  <action>
1. Restructure `src/app/[locale]/page.tsx` to match the required Phase 6 order:
   - hero,
   - product demo section,
   - supported downloaders,
   - benefits,
   - quickstart,
   - plus,
   - FAQ.
2. Add stable section ids for nav anchoring, including `downloaders`.
3. Reduce over-segmentation from repeated divider use. Keep dividers only where they materially improve structure; otherwise use spacing/grouping.
4. Do not leave `AppShowcaseClient` as a standalone top-level screenshot section after this plan.
5. It is acceptable for the new demo and downloaders sections to be temporary imports or placeholders from Plan 03 if the new components are not implemented yet, but the page structure and section ids must already reflect the final architecture.
  </action>
  <verify>
    <automated>cd /Users/tatsuzo/Fork/bitremote-website && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/[locale]/page.tsx` contains `id="downloaders"`
    - `src/app/[locale]/page.tsx` places the downloaders section above benefits and quickstart
    - `src/app/[locale]/page.tsx` does not render `AppShowcaseClient` as a standalone section between hero and benefits
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The homepage skeleton now matches the corrective information architecture and is ready for the new proof and trust sections.</done>
</task>

</tasks>

<verification>
- `npm run lint` passes
- `npm run build` passes and preserves locale routes
- Nav anchors map to existing homepage section ids
- The page order matches the Phase 6 UI contract before detailed content polishing
</verification>

<success_criteria>
1. Navigation clearly communicates current location and available sections
2. Downloaders is restored to the primary nav and homepage structure
3. Hover-only discoverability and bracketed locale switching are removed
4. Homepage structure no longer centers the standalone screenshot showcase
</success_criteria>

<output>
After completion, create `.planning/phases/06-corrective-total-ui-refresh/06-02-SUMMARY.md`
</output>

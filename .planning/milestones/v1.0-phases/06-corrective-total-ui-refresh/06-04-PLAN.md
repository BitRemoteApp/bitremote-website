---
phase: 06-corrective-total-ui-refresh
plan: 04
type: human-verify
wave: 3
depends_on:
  - 06-01-PLAN.md
  - 06-02-PLAN.md
  - 06-03-PLAN.md
files_modified: []
autonomous: false
requirements:
  - DSGN-07
  - DSGN-08
  - NAV-04
  - CONT-06
  - CONT-07
  - BRAND-01

must_haves:
  truths:
    - "Phase 6 design decisions visibly follow the Impeccable context and the site no longer feels like the old interface with a new skin"
    - "No remaining bracket-style CTA, glitch wordmark, or hover-only retro interaction survives in the primary experience"
    - "Supported Downloaders is prominent and product-proof comes from a calm modern demo rather than a screenshot-led section"
    - "The corrected design works across locales, responsive breakpoints, and downloader/legal routes without regressions"
  artifacts:
    - path: "src/app/[locale]/page.tsx"
      provides: "Final corrected homepage structure"
      contains: "id=\"downloaders\""
    - path: "src/components/TextTabsNav.tsx"
      provides: "Final navigation behavior"
      exports: ["TextTabsNav"]
    - path: "src/components/TextButton.tsx"
      provides: "Final CTA primitive with no retro bracket styling"
      exports: ["TextButton"]
---

<objective>
Verify that the corrective redesign actually fulfills the original brief and preserves the site's functional guarantees.

Purpose: Catch the failure mode where the code changes compile but the site still looks structurally or tonally wrong.
Output: verified Phase 6 implementation plus a human approval checkpoint covering visual tone, IA, locale correctness, and regression safety.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.impeccable.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/06-corrective-total-ui-refresh/06-CONTEXT.md
@.planning/phases/06-corrective-total-ui-refresh/06-RESEARCH.md
@.planning/phases/06-corrective-total-ui-refresh/06-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated regression checks for Phase 6 scope</name>
  <files>src/</files>
  <action>
1. Run `npm run lint`.
2. Run `npm run build`.
3. Confirm the homepage still references canonical downloader data and that locale message files remain valid.
4. Grep the codebase for surviving retro primary-experience patterns:
   - decorative CTA brackets,
   - glitch/pixel filter remnants,
   - explicit comments or logic enforcing hover-only active state behavior.
5. Record any failures as blockers, not follow-up nice-to-haves.
  </action>
  <verify>
    <automated>cd /Users/tatsuzo/Fork/bitremote-website && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits 0
    - `npm run build` exits 0
    - no surviving decorative bracket CTA wrappers in shared primary-experience components
    - no surviving glitch/pixel filter implementation in `BitRemoteWordmark`
  </acceptance_criteria>
  <done>Automated checks confirm the corrective redesign compiles and obvious retro regressions are removed.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Validate tone, hierarchy, and cross-locale behavior in the browser</name>
  <files>browser</files>
  <action>
Open the site locally and verify the following pages and conditions:

1. `/en/` at 375px, 768px, and 1280px in dark mode
2. `/en/` at 375px and 1280px in light mode
3. `/ja/` at 375px and 1280px
4. `/zh-hans/` or `/zh-hant/` at 375px
5. one downloader landing page such as `/en/downloaders/aria2/`
6. one legal page such as `/en/privacy/`

Check these specific truths:
- the homepage feels calm, professional, and app-aligned rather than retro or novelty-driven
- the hero and top-of-page proof block are the primary focus, with Supported Downloaders clearly visible near the top
- nav active states, locale selection, and CTA styling feel product-grade without hover-only discovery
- screenshots, if still present, support the story instead of acting as the page's main section
- localized text wraps cleanly, especially on `/ja/` at 375px
- no horizontal scrolling or collapsed layout appears on homepage, downloader pages, or legal pages
  </action>
  <acceptance_criteria>
    - User explicitly approves the corrected visual direction
    - Supported Downloaders placement is accepted as prominent enough
    - The proof/demo section is accepted as stronger than the old screenshot-led structure
    - Japanese mobile layout shows no clipping or overflow
    - Downloader and legal pages remain visually coherent after the shared-component changes
  </acceptance_criteria>
  <done>Human review confirms the corrective redesign fulfills the original brief, not just the code checklist.</done>
</task>

</tasks>

<verification>
- Automated checks must pass before asking for human sign-off
- Human sign-off is required because the core failure mode is visual and structural, not purely technical
</verification>

<success_criteria>
1. Phase 6 passes lint/build and preserves static export behavior
2. The primary experience contains no meaningful retro UI remnants
3. Supported Downloaders and product proof are both prominent and persuasive
4. Human review confirms the site now feels like a true BitRemote UI refresh
</success_criteria>

<output>
After completion, create `.planning/phases/06-corrective-total-ui-refresh/06-04-SUMMARY.md`
</output>

---
phase: 06-corrective-total-ui-refresh
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01-PLAN.md
  - 06-02-PLAN.md
files_modified:
  - src/app/[locale]/page.tsx
  - src/components/AppShowcaseClient.tsx
  - src/components/HeroSection.tsx
  - src/components/BentoCard.tsx
  - src/components/DownloaderLandingPage.tsx
  - src/messages/en.json
  - src/messages/ja.json
  - src/messages/zh-hans.json
  - src/messages/zh-hant.json
autonomous: true
requirements:
  - CONT-06
  - CONT-07
  - BRAND-01
  - DSGN-08

must_haves:
  truths:
    - "The homepage contains a dedicated Supported Downloaders section near the top using canonical product data and linking to landing pages"
    - "Product proof is delivered through a modern interactive or app-like demo rather than a standalone screenshot gallery"
    - "Any screenshot usage becomes supporting evidence inside the new proof/storytelling system rather than the page-defining section"
    - "Benefits, quickstart, pricing, and related surfaces are normalized into the calmer Phase 6 visual language"
    - "Localized homepage copy remains complete across en, ja, zh-hans, and zh-hant"
  artifacts:
    - path: "src/components/AppShowcaseClient.tsx"
      provides: "Repurposed or replaced product-proof component aligned with the corrective redesign"
      exports: ["AppShowcaseClient"]
    - path: "src/app/[locale]/page.tsx"
      provides: "Homepage wiring for the new proof and supported-downloaders sections"
      contains: "id=\"downloaders\""
    - path: "src/messages/en.json"
      provides: "Updated homepage copy needed by the new sections"
      contains: "\"downloaders\""
  key_links:
    - from: "src/app/[locale]/page.tsx"
      to: "src/domain/downloaders.ts"
      via: "supported downloaders section uses canonical downloader data"
      pattern: "supportedDownloaders"
    - from: "src/app/[locale]/page.tsx"
      to: "src/components/AppShowcaseClient.tsx"
      via: "new proof section component"
      pattern: "AppShowcaseClient"
---

<objective>
Restore the missing trust-critical content and replace screenshot-led storytelling with stronger product proof.

Purpose: Deliver the new sections that make the corrected homepage actually persuasive and product-aligned, not just better styled.
Output: a new proof/demo section, a prominent Supported Downloaders section, normalized supporting sections, and any required locale copy updates.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.impeccable.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-corrective-total-ui-refresh/06-CONTEXT.md
@.planning/phases/06-corrective-total-ui-refresh/06-RESEARCH.md
@.planning/phases/06-corrective-total-ui-refresh/06-UI-SPEC.md

@src/app/[locale]/page.tsx
@src/components/AppShowcaseClient.tsx
@src/components/HeroSection.tsx
@src/components/BentoCard.tsx
@src/components/DownloaderLandingPage.tsx
@src/domain/downloaders.ts
@src/domain/downloader-landings.ts
@src/messages/en.json
@src/messages/ja.json
@src/messages/zh-hans.json
@src/messages/zh-hant.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace screenshot-led proof with an app-like demo section</name>
  <files>src/components/AppShowcaseClient.tsx, src/app/[locale]/page.tsx, src/components/HeroSection.tsx</files>
  <read_first>
    - .planning/phases/06-corrective-total-ui-refresh/06-UI-SPEC.md
    - src/components/AppShowcaseClient.tsx
    - src/components/HeroSection.tsx
    - src/app/[locale]/page.tsx
  </read_first>
  <action>
1. Repurpose or replace `AppShowcaseClient` so it becomes the new product-proof section described by the Phase 6 context.
2. The new section must feel like a sober native-app preview:
   - explicit selected/default states,
   - restrained motion only,
   - useful empty/fallback states,
   - no terminal metaphor, no faux command output, no nostalgia cues.
3. Screenshot assets may remain inside this section only as supporting proof. They must not stand alone as the primary narrative device.
4. Integrate the section near the top of the homepage per Plan 02.
5. Keep reduced-motion support intact and avoid any behavior that would break static export.
  </action>
  <verify>
    <automated>cd /Users/tatsuzo/Fork/bitremote-website && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/AppShowcaseClient.tsx` no longer renders a simple centered single-screenshot section with the heading `See it in action`
    - the product-proof component has an explicit empty, fallback, or helper state
    - `src/app/[locale]/page.tsx` renders the new proof section directly after the hero or as part of the hero composition
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The homepage now demonstrates product behavior through a modern proof section instead of a generic screenshot block.</done>
</task>

<task type="auto">
  <name>Task 2: Restore Supported Downloaders and normalize remaining homepage content sections</name>
  <files>src/app/[locale]/page.tsx, src/components/BentoCard.tsx, src/components/DownloaderLandingPage.tsx, src/messages/en.json, src/messages/ja.json, src/messages/zh-hans.json, src/messages/zh-hant.json</files>
  <read_first>
    - src/domain/downloaders.ts
    - src/domain/downloader-landings.ts
    - src/messages/en.json
    - src/app/[locale]/page.tsx
    - src/components/BentoCard.tsx
    - src/components/DownloaderLandingPage.tsx
  </read_first>
  <action>
1. Add a dedicated Supported Downloaders homepage section near the top of the page.
2. Use canonical downloader data and link entries to existing downloader landing pages where applicable.
3. Add or revise localized copy in all four locale files only where needed for the new section and proof block.
4. Normalize surrounding content sections so the homepage reads as one calm system:
   - benefits cards,
   - quickstart cards,
   - pricing/plus section,
   - any downloader-landing shared CTA language affected by shared-component changes.
5. Preserve all existing legal pages, downloader landing routes, and schema-relevant product coverage.
  </action>
  <verify>
    <automated>cd /Users/tatsuzo/Fork/bitremote-website && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - homepage contains a section with `id="downloaders"`
    - the section uses `supportedDownloaders` rather than a duplicated local array
    - each locale message file remains valid JSON
    - downloader landing pages still build successfully
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Supported Downloaders is restored as prominent trust content, and the page's remaining sections match the corrected visual language.</done>
</task>

</tasks>

<verification>
- `npm run lint` passes
- `npm run build` passes for the static export
- All four locale message files remain in sync for any newly introduced keys
- Homepage includes both product proof and downloader trust near the top
</verification>

<success_criteria>
1. Supported Downloaders returns as a first-class homepage section
2. Product proof is interactive/app-like rather than screenshot-led
3. Screenshot assets, if still present, play a supporting role only
4. Locale coverage and downloader landing pages remain intact
</success_criteria>

<output>
After completion, create `.planning/phases/06-corrective-total-ui-refresh/06-03-SUMMARY.md`
</output>

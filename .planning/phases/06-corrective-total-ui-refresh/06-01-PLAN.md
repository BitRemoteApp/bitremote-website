---
phase: 06-corrective-total-ui-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/BitRemoteWordmark.tsx
  - src/components/TextButton.tsx
  - src/components/SectionLabel.tsx
  - src/components/PlatformBadgeStrip.tsx
  - src/components/FaqAccordion.tsx
autonomous: true
requirements:
  - DSGN-07
  - DSGN-08
  - BRAND-01

must_haves:
  truths:
    - "Shared homepage and landing-page primitives follow the Impeccable design context rather than bracket/glitch/uppercase retro language"
    - "The primary CTA no longer renders decorative bracket characters or command-line styling"
    - "The BitRemote wordmark is a static calm brand lockup with no glitch, pixelation, or animated SVG filters"
    - "Section labels, platform badges, and FAQ controls read as polished product UI rather than utility-terminal chrome"
  artifacts:
    - path: "src/components/TextButton.tsx"
      provides: "Calm rounded CTA component with primary and secondary treatments"
      exports: ["TextButton"]
    - path: "src/components/BitRemoteWordmark.tsx"
      provides: "Static wordmark or typographic lockup aligned with the app-like aesthetic"
      exports: ["BitRemoteWordmark"]
    - path: "src/components/SectionLabel.tsx"
      provides: "Sentence-case section heading pattern for Phase 6"
      exports: ["SectionLabel"]
    - path: "src/components/FaqAccordion.tsx"
      provides: "Normalized FAQ row styling without uppercase command affordances"
      exports: ["FaqAccordion"]
  key_links:
    - from: "src/components/HeroSection.tsx"
      to: "src/components/BitRemoteWordmark.tsx"
      via: "hero renders updated brand lockup"
      pattern: "<BitRemoteWordmark"
    - from: "src/components/HeroSection.tsx"
      to: "src/components/TextButton.tsx"
      via: "hero CTA uses the new shared button treatment"
      pattern: "<TextButton"
    - from: "src/components/DownloaderLandingPage.tsx"
      to: "src/components/TextButton.tsx"
      via: "landing-page CTAs inherit the new shared button treatment"
      pattern: "<TextButton"
---

<objective>
Translate the established Phase 6 design context into shared UI primitives before larger structural work begins.

Purpose: Remove the old retro voice from the shared components that still define the site's visible tone.
Output: redesigned shared CTA, static brand lockup, calmer section labels and platform badges, normalized FAQ row treatment, and any small supporting token updates required for those components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.impeccable.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-corrective-total-ui-refresh/06-CONTEXT.md
@.planning/phases/06-corrective-total-ui-refresh/06-RESEARCH.md
@.planning/phases/06-corrective-total-ui-refresh/06-UI-SPEC.md

@src/app/globals.css
@src/components/BitRemoteWordmark.tsx
@src/components/TextButton.tsx
@src/components/SectionLabel.tsx
@src/components/PlatformBadgeStrip.tsx
@src/components/FaqAccordion.tsx
@src/components/HeroSection.tsx
@src/components/DownloaderLandingPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the remaining shared retro brand and CTA primitives</name>
  <files>src/components/BitRemoteWordmark.tsx, src/components/TextButton.tsx, src/app/globals.css</files>
  <read_first>
    - .impeccable.md
    - .planning/phases/06-corrective-total-ui-refresh/06-UI-SPEC.md
    - src/components/BitRemoteWordmark.tsx
    - src/components/TextButton.tsx
    - src/app/globals.css
    - src/components/HeroSection.tsx
  </read_first>
  <action>
1. Replace the current glitch/pixel-filter `BitRemoteWordmark` with a static lockup that reads as a calm product brand mark.
2. Keep the component name `BitRemoteWordmark` so downstream imports remain stable.
3. Remove all animated SVG filters, duplicate reduced-motion fallback text nodes, and any styling that implies retro or novelty rendering.
4. Redesign `TextButton` into a product-grade button/link primitive:
   - no literal `[` or `]`,
   - rounded rectangle or similarly calm app-like shape,
   - primary treatment suitable for App Store CTA,
   - quiet secondary treatment for supporting actions,
   - visible focus state in both themes,
   - sentence-case friendly typography.
5. Add or adjust only the minimum shared CSS variables needed in `globals.css` to support the new button and brand surface language.
6. Do not introduce a new dependency or a parallel "Phase 6 only" CTA component. Replace the existing shared primitive.
  </action>
  <verify>
    <automated>cd /Users/tatsuzo/Fork/bitremote-website && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/BitRemoteWordmark.tsx` does not contain `filter id="pixelate"`
    - `src/components/BitRemoteWordmark.tsx` does not contain `animate attributeName`
    - `src/components/TextButton.tsx` does not contain `[` or `]` as rendered bracket wrappers
    - `src/components/TextButton.tsx` still exports `TextButton`
    - `src/components/TextButton.tsx` supports both `primary` and `secondary` variants
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>Shared brand and CTA primitives no longer communicate retro terminal language.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize section chrome to match the corrective design direction</name>
  <files>src/components/SectionLabel.tsx, src/components/PlatformBadgeStrip.tsx, src/components/FaqAccordion.tsx</files>
  <read_first>
    - .impeccable.md
    - .planning/phases/06-corrective-total-ui-refresh/06-UI-SPEC.md
    - src/components/SectionLabel.tsx
    - src/components/PlatformBadgeStrip.tsx
    - src/components/FaqAccordion.tsx
    - src/app/[locale]/page.tsx
  </read_first>
  <action>
1. Redesign `SectionLabel` so it no longer uses uppercase accent-label styling as the default section heading expression.
2. Redesign `PlatformBadgeStrip` so platform proof reads as calm capability/support metadata rather than small terminal tags.
3. Update `FaqAccordion` row styling to remove uppercase hover wrappers and normalize it to the new surface language established in Task 1.
4. Keep the existing semantics and interaction model:
   - `details/summary` behavior stays intact,
   - platform labels remain accurate,
   - section headings remain reusable and localization-safe.
5. Preserve reduced-motion and accessibility behavior. This task is visual/structural, not behavioral.
  </action>
  <verify>
    <automated>cd /Users/tatsuzo/Fork/bitremote-website && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/SectionLabel.tsx` does not contain `uppercase`
    - `src/components/PlatformBadgeStrip.tsx` does not contain `uppercase`
    - `src/components/FaqAccordion.tsx` does not contain `uppercase`
    - `src/components/FaqAccordion.tsx` still renders `details` and `summary`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Shared section chrome reflects the calm, product-first Phase 6 direction and is ready for page-level structural work.</done>
</task>

</tasks>

<verification>
- `npm run lint` passes with 0 warnings
- `npm run build` preserves static export output
- No shared component introduced by this plan renders decorative bracket wrappers
- The wordmark no longer relies on motion or glitch effects to render correctly
</verification>

<success_criteria>
1. Shared CTA, wordmark, and section primitives visibly reflect the Impeccable design context
2. Decorative brackets and glitch/pixel effects are removed from shared primitives
3. Existing imports remain stable and page code does not need temporary compatibility shims
4. Lint and build both pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-corrective-total-ui-refresh/06-01-SUMMARY.md`
</output>
